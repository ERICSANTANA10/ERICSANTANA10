name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      ```

### O que mudou nacontents: write
    runs-on: ubuntu-latest lógica das cores:
*   **Fundo Azul Neon**: Mantive os três primeiros níveis como `#00ffff` (azul neon/ciano) para garantir que o
    timeout-minutes: 5

    steps: mapa continue vibrante
      - name: generate github-contribution-grid-.
*   **Comida Laranja**: O penúltimo nível agora é `#ffa500`.
*   **Comida Vermelha**: O último nível (para os dias em que você programar muito!) agora é `#ff0000` (vermelho vivo).
*   **Cobra Roxa**: Sua cobrinha continua sendo o destaque em `#bc13fe`snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#bc13fe&color_dots=#00ffff,#00ffff,#00ffff,#ffa500,#ff0000
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#bc13fe&color_dots=#00ffff,#00ffff,#00ffff,#ffa500,#ff0000

      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
          keep_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
